---
import BaseLayout from "../layouts/BaseLayout.astro";
import SiteHeader from "../components/SiteHeader.astro";
import SiteFooter from "../components/SiteFooter.astro";
import TechnicalNav from "../components/TechnicalNav.astro";
import { evidenceState, missingServiceEvidence } from "../data/service-evidence";
---

<BaseLayout routeKey="authorization">
  <div class="page-frame">
    <SiteHeader />
    <main id="main-content" class="content-page">
      <div class="content-shell contract-shell">
        <header class="content-hero">
          <h1>Autorisierung</h1>
          <p>Aktuell existiert kein öffentlicher Auth-Flow auf seomcp.de.</p>
          <p class="page-meta">{evidenceState}</p>
        </header>
        <div class="content-blocks">
          <section class="content-block"><h2>Vor Veröffentlichung erforderlich</h2><ul>{missingServiceEvidence.authorization.map((item) => <li>{item}</li>)}</ul></section>
          <p class="launch-note">Unzulässige, widerrufene oder tenantfremde Zugriffe müssen reproduzierbar scheitern.</p>
        </div>
        <TechnicalNav />
      </div>
    </main>
    <SiteFooter />
  </div>
</BaseLayout>
